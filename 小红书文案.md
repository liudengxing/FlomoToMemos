# 🚀 全网首发！flomo 笔记一键搬家到 memos，连图片和时间都原封不动！

用过 flomo 的朋友应该都懂——写了几百条笔记，想迁到自部署的 memos，结果发现**官方根本不支持互相导入** 😭

网上能找到的脚本都是老版 memos API，新版 v1 早改了，一跑全是 404，时间也不对。

于是我**自己动手魔改了一个开源工具**，亲测跑了 **537 条笔记 + 47 张图片** 完美导入！

---

## ✨ 它做了什么

🔹 **解析 flomo 导出的 HTML**，提取每条笔记的内容、标签、时间、附件路径
🔹 **调用 memos v1 最新 API**，路径从 `/memo` 换成了 `/memos`，附件从 `resource/blob` 换成了 `attachments`
🔹 **保留原始创建时间**，不会全部变成"今天"
🔹 **图片附件也一起搬**，自动 base64 上传并挂到对应笔记上
🔹 **自带去重逻辑**，补图片也不会重复建 memo

---

## 🛠 三步搞定

### 1️⃣ flomo 导出
flomo → 设置 → 导出 → 下载 HTML，放到 `flomo/` 文件夹

### 2️⃣ 改两处配置
- `memos/api.py`：把 Host 改成你的 memos 地址
- `token.txt`：填 memos Access Token

### 3️⃣ 两行命令
```bash
pip install beautifulsoup4 requests
python servicer.py     # 解析 flomo HTML
python controller.py   # 上传到 memos
```

---

## 🔗 开源地址

GitHub：**github.com/liudengxing/FlomoToMemos**

README 写了完整步骤 + API 兼容变化说明，小白也能跑 ✌️

如果你也有几百条 flomo 笔记想搬家，直接 clone 下来改个 Host 和 Token 就能用，不用折腾！

---

## 💡 踩过的坑也写进 README 了

- memos v1 把 `createdTs` 改成了 `createTime`（ISO 8601 字符串）
- 附件上传从 multipart 变成了 base64 JSON
- 创建 memo 和上传附件要分开，先建 memo 再挂附件

这些坑我都替你踩过了 😂

---

#memos #flomo #自托管 #开源工具 #笔记迁移 #NAS #docker #效率工具 #程序员日常 #数字花园
